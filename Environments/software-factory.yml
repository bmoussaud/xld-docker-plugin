registry:
  image: registry
  ports:
    - "5000:5000"
jenkins:
  build: ../Dockerfiles/jenkins
  privileged: true
  ports:
    - "8080:8080"
  volumes:
    - "/var/run/docker.sock:/var/run/docker.sock"
  links:
    - registry


